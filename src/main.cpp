#include <iostream>

#include "pool.hpp"
#include "arena.hpp"

int main() {
    // fixed_pool<int, 30, 2> pool;
    // int* a = pool.malloc();
    // int* b = pool.malloc();
    // int* c = pool.malloc();
    // int* d = pool.malloc();
    // *a = 11;
    // *b = 420;
    // *c = 1010002;
    // *d = -20;
    // pool.free(a);
    // pool.free(b);
    // pool.free(c);
    // pool.free(d);

    // arena<256> arena;
    // int* a = arena.malloc<int>(1);
    // int* b = arena.malloc<int>(1);
    // char* c = arena.malloc<char>(1);
    // long* d = arena.malloc<long>(1);
    return 0;
}